import React from 'react';

import SectionThree from './SectionThree';

export default {
  title: 'Task #1/SectionThree',
  component: SectionThree
};

const Template = () => (
  <SectionThree />
);

export const Default = Template.bind({});
