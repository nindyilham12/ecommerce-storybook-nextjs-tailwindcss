import React from 'react';

import SectionOne from './SectionOne';

export default {
  title: 'Task #1/SectionOne',
  component: SectionOne
};

const Template = () => (
  <SectionOne />
);

export const Default = Template.bind({});
